import type { EntryCategory } from "./types";

/** Categories where long-form / multiline text input and display is used */
export const MULTILINE_CATEGORIES = new Set<EntryCategory>(["note", "grammar"]);
